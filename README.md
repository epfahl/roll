A silly Rust CLI that takes a DND die, plays a sound, and rolls for a value.

Example:

`cargo run d20`

> You rolled 17!

Because of the sound file, this doesn't work as a standalone executable binary.
